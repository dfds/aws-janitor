---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: iamroleservice
  name: iamroleservice
  namespace: $(kubernetes-namespace)
spec:
  ports:
    - name: external
      port: 80
      targetPort: web
  selector:
    app: iamroleservice
  type: ClusterIP
